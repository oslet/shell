#!/bin/bash
for file in /tmp/*
do
    if [ -f ${file} ]
        then
        if [ -e "${file}.bak" ]
            t
            echo "Error:Skipping ${file}.bak as it already exists"
            elseget hosts
            echo "Backing up $fi
            cp "${file}" "${file}.nt ent
            fi
        fi
    done
